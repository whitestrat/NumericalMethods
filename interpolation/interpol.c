#include <stdio.h>
#include <math.h>

float f(float x){
    float sin = pow(sin(x/5), 2);
    return 1/2*(exp(sin) - exp(-sin));
}

float* make_points(float (*f)(float), int N){
    
}

float* interpolate_L(float **points, int N){
    

}

float* interpolate_N(float **points, int N){


}
int main(){

    float points[]
    return 0;
}